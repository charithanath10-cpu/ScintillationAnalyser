# Standalone scintillation analysis scripts (not part of the Streamlit app).
# This __init__.py prevents this directory from accidentally being treated
# as an implicit namespace package and leaking into the app's import path.
